services:
  workbench:
    build: .
    command: ["serve", "/workspace", "--host", "127.0.0.1", "--port", "8765"]
    volumes:
      - ./workspaces/demo:/workspace
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
