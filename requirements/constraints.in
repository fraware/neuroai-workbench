# Hashed third-party constraints for CI/reproducible installs.
# Does not include the local neuroai-workbench package; install that with --no-deps -e .
# Pin Linux/Windows/older-Python transitive backends so Windows-compiled hashes still
# install across the CI matrix (3.10-3.14).
SecretStorage>=3.2,<4
jeepney>=0.6,<1
backports.tarfile>=1.2,<2
exceptiongroup>=1,<2
importlib-metadata>=4.11.4,<10
rpds-py>=0.22,<2026.5
jsonschema>=4.26,<5
build>=1.3,<2
coverage[toml]>=7.10,<8
mypy>=1.17,<3
types-jsonschema>=4.26,<5
pip-audit>=2.9,<3
pytest>=9.0,<10
pytest-cov>=7,<8
ruff>=0.12,<1
twine>=6.1,<8
