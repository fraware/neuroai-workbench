# Deferred human governance overlay

Issue [#101](https://github.com/fraware/neuroai-workbench/issues/101) holds the remaining canonical release-authority layer. Core engineering ([#43](https://github.com/fraware/neuroai-workbench/issues/43)) is complete. For a team of ≤5, shadow residual review is a short local procedure — not an institutional multi-role ceremony.

## Status

| Layer | State |
| --- | --- |
| Non-canonical core cycle (#43) | Complete |
| Small-team dual review + formal disposition | Available; Wave 2 formal disposition `NO_GO` |
| Typed URL residual | `KEEP_AS_TYPED_FAILURE`; `finding_effect = NONE` |
| Canonical `AUTHORIZED` / `PUBLISHED` successor | Deferred to #101 |

Archive/data-store boundary ([#44](https://github.com/fraware/neuroai-workbench/issues/44)) and successor-generation mechanics ([#41](https://github.com/fraware/neuroai-workbench/issues/41)) are complete. They do not authorize a published canonical release.

## Small-team residual

See [shadow-refresh-evaluation.md](shadow-refresh-evaluation.md#small-team-residual-101) for the procedure: `status → opinion A/B → assess → formal-disposition`.

Fail-closed rules: claimed local IDs only; never forge `GO`; never invent `FAIL` from typed HTTP failures; never write a canonical successor from shadow evaluation.

## What #101 still covers

- authorization gates for `AUTHORIZED` and `PUBLISHED` canonical successors;
- binding of exact candidate, delta, reopening, product, and release hashes to attributable owner and release-authority decisions;
- an additive governance overlay that does not rewrite core evidence or historical records.

## Boundaries retained

- historical and predecessor objects remain immutable;
- protected evidence remains outside public Git;
- retrieval failures remain typed operational outcomes;
- shadow evaluation does not mutate assessments;
- successors and products remain explicitly non-canonical until #101;
- substantive scientific, regulatory, clinical, conformance, institutional, and UNESCO claims remain withheld.
