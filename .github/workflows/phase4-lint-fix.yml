name: Phase 4 temporary lint helper (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled_audit_marker:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain disabled state
        run: echo "Temporary migration lint helper is disabled after deterministic Ruff repair."
